[project]
name = "docspec"
version = "0.2.0"
description = "Format-neutral bulk document acquisition, processing, segmentation, and release management"
requires-python = ">=3.12,<3.13"
dependencies = [
    "jsonschema>=4.23,<5",
    "rulespec-artifacts==1.0.0",
]

[tool.uv.sources]
rulespec-artifacts = { path = "vendor/rulespec_artifacts-1.0.0-py3-none-any.whl" }

[project.optional-dependencies]
http = [
    "httpx>=0.27,<1",
]
s3 = [
    "boto3>=1.34,<2",
]
pdf = [
    "pymupdf>=1.24,<2",
    "pypdf>=5,<7",
]
dagster = [
    "dagster>=1.13,<2",
]
[dependency-groups]
dev = [
    "httpx>=0.27,<1",
    "ijson>=3.3,<4",
    "jsonschema>=4.23,<5",
    "pytest",
    "ruff>=0.12,<0.13",
]

[project.scripts]
docspec = "docspec.entrypoint:main"

[build-system]
requires = ["uv_build<0.12"]
build-backend = "uv_build"

[tool.pytest.ini_options]
addopts = "--strict-config --strict-markers -m 'not integration'"
testpaths = ["tests"]
markers = [
    "integration: live external-service verification",
]

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
    "docs",
]
