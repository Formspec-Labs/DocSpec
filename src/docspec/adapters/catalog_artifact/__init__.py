"""Immutable source-catalog construction, reading, and verification.

The public API remains in docspec.source_catalog. Modules here own the
wire rules, bounded inputs, derivation engines, and publication lifecycle.
"""
